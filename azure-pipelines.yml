# ==========================================
# CI/CD Pipeline
# GitHub → Docker → ACR → AKS
# Development → QA → Production
# ==========================================

trigger:
- main


variables:

  dockerRegistryServiceConnection: '8072af9b-e881-4d56-9d7e-3b46abcb6e8b'

  azureServiceConnection: 'azure-service-connection'

  resourceGroup: 'rg-devops-project'

  aksCluster: 'aks-devops-project'

  imageRepository: 'pankajkumardevdevopsrealtimeproject'

  containerRegistry: 'pankajacr12345.azurecr.io'

  dockerfilePath: '$(Build.SourcesDirectory)/BankingApp/Dockerfile'

  tag: '$(Build.BuildId)'

  vmImageName: 'ubuntu-latest'


stages:


# ==========================================
# STAGE 1 - BUILD AND PUSH
# ==========================================

- stage: Build
  displayName: 'Build and Push to ACR'

  jobs:

  - job: BuildDockerImage
    displayName: 'Build Docker Image'

    pool:
      vmImage: $(vmImageName)

    steps:

    - checkout: self
      displayName: 'Checkout Source Code'

    - task: Docker@2
      displayName: 'Build and Push Docker Image to ACR'

      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfile: $(dockerfilePath)
        containerRegistry: $(dockerRegistryServiceConnection)

        tags: |
          $(tag)


# ==========================================
# STAGE 2 - DEVELOPMENT
# ==========================================

- stage: Development
  displayName: 'Deploy to Development'

  dependsOn: Build

  jobs:

  - deployment: DeployToDevelopment
    displayName: 'Deploy to Development'

    environment: 'Development'

    pool:
      vmImage: $(vmImageName)

    strategy:
      runOnce:
        deploy:
          steps:

          - checkout: self
            displayName: 'Checkout Repository'

          - task: KubernetesManifest@1
            displayName: 'Deploy Application to Development'

            inputs:

              action: deploy

              connectionType: azureResourceManager

              azureSubscriptionConnection: $(azureServiceConnection)

              azureResourceGroup: $(resourceGroup)

              kubernetesCluster: $(aksCluster)

              # 👇 DEVELOPMENT NAMESPACE
              namespace: development

              manifests: |
                $(Build.SourcesDirectory)/k8s/configmap-dev.yaml
                $(Build.SourcesDirectory)/k8s/deployment.yaml
                $(Build.SourcesDirectory)/k8s/service.yaml

              containers: |
                $(containerRegistry)/$(imageRepository):$(tag)


# ==========================================
# STAGE 3 - QA
# ==========================================

- stage: QA
  displayName: 'Deploy to QA'

  dependsOn: Development

  jobs:

  - deployment: DeployToQA
    displayName: 'Deploy to QA'

    environment: 'QA'

    pool:
      vmImage: $(vmImageName)

    strategy:
      runOnce:
        deploy:
          steps:

          - checkout: self
            displayName: 'Checkout Repository'

          - task: KubernetesManifest@1
            displayName: 'Deploy Application to QA'

            inputs:

              action: deploy

              connectionType: azureResourceManager

              azureSubscriptionConnection: $(azureServiceConnection)

              azureResourceGroup: $(resourceGroup)

              kubernetesCluster: $(aksCluster)

              # 👇 QA NAMESPACE
              namespace: qa

              manifests: |
                $(Build.SourcesDirectory)/k8s/configmap-qa.yaml
                $(Build.SourcesDirectory)/k8s/deployment.yaml
                $(Build.SourcesDirectory)/k8s/service.yaml

              containers: |
                $(containerRegistry)/$(imageRepository):$(tag)


# ==========================================
# STAGE 4 - PRODUCTION
# ==========================================

- stage: Production
  displayName: 'Deploy to Production'

  dependsOn: QA

  jobs:

  - deployment: DeployToProduction
    displayName: 'Deploy to Production'

    # Production Approval Check is attached here
    environment: 'Production'

    pool:
      vmImage: $(vmImageName)

    strategy:
      runOnce:
        deploy:
          steps:

          - checkout: self
            displayName: 'Checkout Repository'

          - task: KubernetesManifest@1
            displayName: 'Deploy Application to Production'

            inputs:

              action: deploy

              connectionType: azureResourceManager

              azureSubscriptionConnection: $(azureServiceConnection)

              azureResourceGroup: $(resourceGroup)

              kubernetesCluster: $(aksCluster)

              # 👇 PRODUCTION NAMESPACE
              namespace: production

              manifests: |
                $(Build.SourcesDirectory)/k8s/configmap-production.yaml
                $(Build.SourcesDirectory)/k8s/deployment.yaml
                $(Build.SourcesDirectory)/k8s/service.yaml

              containers: |
                $(containerRegistry)/$(imageRepository):$(tag)